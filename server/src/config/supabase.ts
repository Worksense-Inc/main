// Supabase runtime client removed for minimal database setup.
// This stub remains only to avoid breaking imports if referenced inadvertently.
// If you need a client later, reintroduce @supabase/supabase-js and restore the previous implementation.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabase: any = undefined;
export default supabase;
